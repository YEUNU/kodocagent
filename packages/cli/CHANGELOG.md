# kodocagent

## 0.7.0

### Minor Changes (kordoc API-우선 · 자가검증 루프 · API 사용량 표시)

- **에이전트 자가 검증 루프 — 모델이 약해도 요청을 끝까지 처리** — 문서를 변경한 응답 끝에 에이전트가 **변경 결과를 자동으로 다시 읽고 원래 요청을 항목별로 점검**해, 빠진 부분이 있으면 스스로 마저 수정합니다(없으면 "검증 완료"). "모든/전부" 같은 일괄 요청에서 일부만 처리하고 끝내는 문제를 구조적으로 보정합니다. (실측: gpt-5.4 코어 18스펙 12→15 통과, 날짜 일괄 통일 완결성 실패가 해소.)
- **API 사용량·추정 비용 표시** — 응답 끝과 종료 시 **누적 입력·출력 토큰과 추정 비용**(USD)을 보여줍니다. `/usage`(=`/cost`) 명령으로 언제든 확인할 수 있습니다. (Claude opus/sonnet/haiku 정가 추정. 단가 미등록 모델은 토큰만 표시.)
- **편집 엔진을 kordoc 무손실 프리미티브로 이전** — 찾기·바꾸기(`propose_find_replace`), 개인정보 마스킹(`propose_redact_pii`), 표 셀 편집(`propose_cell_edit`)이 자체 XML 정규식/토크나이저 대신 kordoc `scanSectionXml`(소스맵) + `buildRangeSplices`/`buildParagraphSplices`(run·서식 보존 범위 치환) + `applySplices`로 동작합니다. **부가 효과**: 텍스트가 여러 서식 런에 나뉘어 있어도(굵게 등 부분 서식) 찾기·바꾸기와 PII 마스킹이 경계를 가로질러 처리됩니다(기존엔 누락). 병합 셀(cellSpan/rowSpan) 구조는 그대로 보존됩니다.
- **새 기능: 문서 내보내기(`export_document`)** — 문서를 HTML로 내보냅니다(kordoc `renderHtml`, 항상 가능). PDF는 puppeteer-core가 설치된 환경에서 가능하며, 없으면 친절히 안내합니다.
- **양식 필드 안내 강화** — `propose_form_fill`이 kordoc `extractFormSchema`로 채울 수 있는 필드와 타입(텍스트·날짜·전화·금액 등)을 파악해, 라벨이 안 맞으면 실제 채울 수 있는 필드 목록을 알려줍니다.
- **포맷 감지를 kordoc에 위임** — .hwp(OLE2)·.hwpx(ZIP) 매직바이트 판별을 자체 구현 대신 kordoc `isOldHwpFile`/`isZipFile`로 통일했습니다.
- **읽기 텍스트 손실 가드 확장** — 0.6.3의 '표입니다' 무성 손실 복원 가드를 마크다운뿐 아니라 **문서 구조(blocks)에도 적용** — 양식 필드 인식 등 blocks 기반 기능까지 일관되게 보호합니다.

### 검증

- 마이그레이션 산출물(find-replace·cell-edit 좌표+라벨·redact-pii)을 **실제 한컴오피스 뷰어로 열어 무손상 확인**. 서식 분리 텍스트·병합셀 보존 회귀 테스트 추가.
- **실 한국 문서로 LLM 검증** — 실제 수능 사회·문화 문제지(.hwpx, 중첩표·이미지)를 LLM이 편집(○○신문→한국경제신문)하고 한컴에서 무손상 확인. 자가 검증 루프가 함께 동작.
- 전체 테스트 501 통과(build·typecheck·lint 그린).

## 0.6.3

### Patch Changes (문서 텍스트 무성 손실 수정 · 양식 채우기 견고화)

- **문서 본문 텍스트가 조용히 사라지던 문제 수정** — 일부 한국어 문서에서 `…목표입니다`, `…번호입니다`, `…발표입니다`, `…도표입니다`처럼 흔한 종결어의 꼬리가 읽기 결과에서 누락되던 버그를 수정했습니다(문서 도형/이미지 설명을 지우는 내부 규칙이 같은 글자로 끝나는 일반 단어까지 잘라낸 것이 원인). 텍스트 손실뿐 아니라 그 문서를 편집할 때 본문에 잉여 텍스트가 끼어드는 2차 손상도 유발했습니다. 발행 번들에 **런타임 복원 가드**를 포함해(원본에 실재하는 텍스트만, 정확 일치 시에만 되돌림 — 날조·거짓 양성 없음) 보호합니다. 실제 한컴오피스 뷰어로 수정 결과가 깨끗이 열리는 것을 육안 검증했습니다.
- **양식 채우기 견고화** — `propose_form_fill`이 kordoc 전용 `fillHwpx`(HWPX 원본 직접 수정, 서식 100% 보존)로 동작합니다. 기존의 마크다운 정규식 치환 우회를 제거해, 미매칭 라벨 경고가 정확해지고 채운 필드가 없으면 명확히 알립니다.

### 내부 변경

- 실제 한컴오피스 뷰어 열림 검증 하네스 추가(맥 제어로 편집 산출물을 직접 열어 무손상 확인) + 위 버그의 회귀 가드 테스트. 발행 번들 미포함(devDependency).

## 0.6.2

### Patch Changes (능력·한계 grounding)

- **능력·한계 인식(요청 grounding)** — 에이전트가 사용자 요청을 시스템의 **실제 능력 기준으로 판단**합니다. 현재 도구로 할 수 없는 작업(글꼴·굵게·줄간격 등 문자/문단 서식과 제목 스타일 일관화, 머리말/꼬리말·쪽번호 레이아웃, 각주 삽입·형식, 목차·개요번호 자동 갱신, 그림·표 번호 상호참조, 이미지·도형, 중첩표 안쪽 셀)을 요청받으면 **어설프게 시도하지 않고 한계를 솔직히 알린 뒤** 가능한 부분만 진행하거나 대안을 안내합니다. "가능한 작업" 목록은 등록된 도구에서 **자동 도출**되어 도구 추가/변경 시 드리프트 없이 반영됩니다. (실모델 e2e로 검증)

### 내부 변경

- 의도충실도 검증 하네스 강화 — 실제 `.hwpx` XML 직접 검사(양식 개체 값·표 행 등 kordoc 마크다운이 드롭하는 항목) + LLM-judge. 발행 번들 미포함(devDependency).

## 0.6.1

### Patch Changes (에이전트 편집 행동 개선 · `.hwp` 구조편집 가드)

- **편집 행동·완결성 규칙 강화** — 에이전트가 (1) 모델 학습 시점 이후의 최신 법령·기준·수치 등 **확실히 알 수 없는 정보는 추측·날조하지 않고 사용자에게 먼저 질문**하고(필요 시 `korean-law` 등 도구로 확인·출처 표기), (2) 수정 요청에 분석·목록만 출력하지 않고 **실제 `propose_*`로 수정안을 제안**하며, (3) "모두/일괄" 수정은 **빠짐없이 처리하고 적용 후 누락을 자가확인**하도록 시스템 규칙을 추가했습니다. 실모델 e2e로 의도충실도를 검증했습니다(claude-sonnet-4-6 원문 프롬프트 6/6 안정).
- **`.hwp` 구조편집 안내 가드** — 표·셀·양식·찾기바꾸기 등 `.hwpx` 기반 구조 편집 툴에 `.hwp`(HWP5 바이너리)를 주면, 원인과 해결 방법(본문은 `propose_edit`, 구조 편집은 한글에서 `.hwpx`로 저장 후)을 담은 안내를 반환하고 안전하게 중단합니다(이전엔 내부 오류로 실패).

### 내부 변경

- 문서 편집 의도충실도 **검증 하네스**(`packages/doc-tools/src/eval/`) 추가 — 합성·실문서 fixture + 실모델 e2e(멀티모델). 발행 번들에는 미포함(devDependency).

## 0.6.0

### Minor Changes (한글 `.hwp` 직접 편집 · 무손실 편집 — kordoc 3.x)

- **한글(`.hwp`) 직접 편집** — `propose_edit`가 `.hwp`(HWP5 바이너리)를 **원본 `.hwp` 형식 그대로 제자리 편집**합니다(kordoc 3.x `patchHwp`). 이전에는 `.hwp` 편집 결과를 `.hwpx`로 변환해 저장했으나(어떤 경로로도 `.hwp` 출력 불가했던 차단 항목), 이제 변환 없이 원본 형식을 유지합니다. 안전하게 적용할 수 없는 변경은 보수적으로 건너뛰고 경고합니다. (단, 표·셀·찾기바꾸기 등 구조 편집 툴은 여전히 `.hwpx` 기반)
- **무손실 편집 패치** — `propose_edit`(`.hwpx`)와 `propose_form_fill`이 마크다운 전체 재생성 대신 **무손실 패치(`patchHwpx`)**로 동작해 서식·구조 보존성이 향상됐습니다.

### 내부 변경

- 의존성 마이그레이션: `@clazic/kordoc@2.7.6` → `kordoc@3.1.1`(활성 후속). 공개 동작 무회귀(게이트: build·typecheck·lint 0, test 384 통과, 실문서 e2e로 `.hwp`/`.hwpx` 편집 검증).
- 정리: 마이그레이션 잔여 데드코드·미사용 의존성(`core`→`doc-tools`)·구버전 주석 제거, 추적되던 `node_modules` 심링크 정리.

## 0.5.0

### Minor Changes (개인정보 비식별 · 구조적 검색)

- **개인정보 비식별 처리(`propose_redact_pii`)** — 문서의 주민등록번호·전화번호·이메일·신용카드번호를 마스킹 처리합니다. `.hwpx`는 XML 직접 패치로 표·이미지·서식을 보존하며 `.md`/`.txt`도 지원합니다. 원문 값은 미리보기·응답에 노출하지 않고, 승인 후에만 저장됩니다(정보공개·비식별 워크플로). "개인정보 가려줘/비식별 처리해줘"로 동작하며, "확인만"은 `scan_pii`(읽기 전용)로 처리합니다.
- **구조적 위치 검색(`find_in_document`)** — 큰 문서에서 특정 텍스트가 어느 표의 어느 셀(`tableIndex`·`row`·`col`)에 있는지, 또는 본문 어디에 있는지 찾아 줍니다. 전체를 읽지 않고 수정 대상을 정확히 지정해 토큰을 아끼고 정확도를 높입니다. 반환 좌표는 `propose_cell_edit`과 완전 호환(실문서 e2e로 단일·멀티표 좌표 일치 검증).

## 0.4.5

### Minor Changes (안전·보존 가드)

- **편집 안전 규칙** — 에이전트가 요청한 부분만 수정하고, 문서에 없는 정보(수치·인명·날짜·금액·계약 조항)를 지어내지 않으며, 숫자·날짜·단위·고유명사·인용부호 내부·법령 조문 번호를 보존하고, 전문 용어를 임의 치환하지 않으며, 계약서·약관·공시 등 법적·공식 문서는 지정한 변경만 수행하도록 시스템 규칙을 추가했습니다.
- **개인정보 점검(`scan_pii`)** — 문서에서 주민등록번호·전화번호·이메일·신용카드번호를 탐지해 마스킹된 형태로 보고합니다(원문 값 미노출). 외부 공유 전 점검에 사용하세요.
- **개인정보 승인 경고** — 수정 제안의 변경 영역에 개인정보가 포함되면 승인 단계 경고에 자동으로 표시합니다.

### Patch Changes (버그 수정)

- **작업 문서 기억(openDocuments) 복원** — 0.4.3에 추가됐으나 시스템 프롬프트 생성 시점 문제로 "열람한 문서"가 항상 비어 있던 버그를 수정(이전 턴 기록에서 복원). `write_new_spreadsheet` 경로 기록 누락도 보완.
- **`restore_backup` 모호 매칭 수정** — 파일명 대신 파일 이름만 지정하면 가장 오래된 백업이 조용히 선택되던 문제를, 정확한 파일명 우선·부분일치 다중 시 최신 선택+경고로 수정.

## 0.4.4

### Patch Changes (UX 다듬기)

- **`--print`(단발 질의) 읽기 전용화** — 비대화형 모드에서 쓰기 도구(`propose_*`/`write_new_*`/`restore_backup`)를 아예 등록하지 않습니다. 기존엔 쓰기 도구가 노출돼 모델이 수정안을 제안했다가 자동 거절되며 한 턴을 낭비했습니다.
- **찾기/바꾸기 폴백 안내** — 서식이 나뉜 텍스트가 일부 교체되지 않을 때, 경고에 "표 셀이면 `propose_cell_edit`, 본문이면 한글에서 직접 수정"이라는 처리 방법을 함께 안내합니다.
- **능력 소개** — `/help`에 "할 수 있는 일"(문서 읽기·표/양식 수정·찾기바꾸기·되돌리기·법령 검토) 안내를 추가하고, 채팅 시작 시 한 줄 힌트를 표시합니다.
- **시트 편집 `.xls` → `.xlsx` 변환** — `propose_sheet_edit`이 `.xls` 파일을 받으면 xlsx 형식으로 `.xlsx` 경로에 저장하고 원본 `.xls`는 보존합니다(기존엔 xlsx 내용을 `.xls` 이름으로 제자리 덮어써 포맷이 어긋났음). `.hwp → .hwpx` 정책과 동일.

## 0.4.3

### Minor Changes (에이전틱 문서편집 UX)

- **되돌리기(undo/restore)** — `list_backups`로 백업 목록을 조회하고, `restore_backup`으로 이전 백업으로 복원합니다. 복원도 승인을 거치며, **복원 전 현재 상태가 자동 백업**되어 복원 자체도 되돌릴 수 있습니다. (그동안 커밋마다 백업은 쌓였지만 복원 수단이 없었음 — "되돌려줘"가 이제 동작합니다.)
- **작업 문서 기억** — 에이전트가 이번 세션에서 열람·수정한 문서 경로를 기억합니다(`read_document`/`compare_documents`/`write_new_document`). 여러 턴에 걸친 느슨한 수정 요청에서 "작업 중인 파일"을 잃지 않습니다. (이전엔 `openDocuments`가 채워지지 않아 항상 비어 있었음.)
- **찾기/바꾸기 diff 미리보기** — `propose_find_replace`의 승인 단계가 한 줄 요약 대신 **매치별 `- 이전 / + 이후` 스니펫**(최대 20곳, 긴 문장은 변경 구간만 잘라 표시)을 보여줍니다. 문서 전체 치환을 눈으로 확인하고 승인할 수 있습니다.

### 검증

- 실모델 end-to-end: 신규 도구 등록·스키마 변환 → "되돌려줘" → `list_backups`→`restore_backup`→커밋, 디스크에서 원본 복원·안전백업 생성 확인.
- 실모델 end-to-end: 실제 보도자료 `'국민주권'→'국민중심'` 8곳 번호별 diff 미리보기 + cross-run 1곳 미치환 경고 동시 표기.

## 0.4.2

### Patch Changes

- **제안 경고를 커밋 결과에 포함 — 에이전트의 정직 보고** — 찾기/바꾸기 등에서 서식이 나뉜 텍스트가 일부 미치환되면, 그 경고가 이제 모델에 전달되어 "총 9곳 중 8곳 교체, 1곳은 서식 분리로 실패"처럼 정직하게 보고된다(기존엔 도구 경고가 누락돼 "모두 변경"으로 과장될 수 있었음).

### 검증

- 느슨한 자연어 지시(위치·셀 미지정) end-to-end 재검증 — 위치 추론·캐주얼 표현 해석·모호 시 되물음 정상.
- 교차문서 검토(별도 규정 파일로 현재 문서 점검) end-to-end 검증 — 실제 보도자료 + 작성지침으로, 위반 1건 정확 적발·준수 4건 정확 판정(재현율·정밀도 확인).

## 0.4.1

### Patch Changes (긴급 수정)

- **에이전트 구동 불능 버그 수정(critical)** — 0.4.0의 `propose_cell_edit` 스키마가 `z.undefined()`를 사용해 AI SDK의 JSON Schema 변환이 실패, 채팅 첫 메시지부터 "Undefined cannot be represented in JSON Schema" 오류로 에이전트가 동작하지 않던 문제를 해결(전 주소 필드 optional 단일 스키마 + 핸들러 검증).

### Minor Changes

- 문서 전체 찾기/바꾸기(`propose_find_replace`) — 본문·표·머리말 등 모든 텍스트를 문서 XML 직접 수정으로 치환. 이미지·표·서식 보존, 복잡 문서에서도 안전(`.hwpx` 전용). 서식이 나뉜 텍스트는 일부 누락 시 안내.
- 표 행·열 편집(`propose_table_structure`) — 행·열 추가/삭제·셀 병합을 XML 직접 수정으로. anchor 텍스트로 표 지정, 기존 병합 가로지름은 안전 거부, 이미지·다른 표 보존.

### 내부 변경

- rhwp(`@rhwp/core`) 의존 제거 — 찾기/바꾸기·표 구조를 자체 XML 패치로 구현(rhwp가 복잡 문서를 손상시키는 문제 회피). 사용자 설치 시 WASM(5.6MB) 미포함.
- 실제 공문서(보도자료·지자체 양식)로 AI end-to-end 수정 검증 완료.

## 0.4.0

### Minor Changes

- `propose_cell_edit` 추가 — HWPX 표 셀을 내부 XML에서 직접 수정. 병합셀(cellSpan/rowSpan)·서식·다른 셀·양식 개체를 완전히 보존(기존 `propose_edit`의 마크다운 왕복은 병합 구조를 평면화·소실). 셀은 표 좌표 또는 라벨(인접 방향)로 지정, `expectedText` 안전 검증, 여러 셀 원자적 수정
- 양식 빈 칸 채우기 — 비어 있는 표 셀(`<hp:t/>`)에 값 입력 지원
- 라벨 기반 셀 지정 — "성명" 옆/아래 칸처럼 라벨+방향(right/below)으로 값 셀 지정(병합 span 고려)
- 양식 개체(Form Object) 지원 — `list_form_objects`로 입력상자·누름틀·콤보박스·체크/라디오 단추 5종을 조회하고, `propose_form_object`로 값 설정(편집상자 텍스트, 체크/라디오 on/off, 콤보 선택, 단추 캡션). 타입·콤보 항목·기대값 검증, 원자적 적용

### 참고

- `propose_cell_edit`·양식 개체 툴은 `.hwpx` 전용. `.hwp`는 한글에서 `.hwpx`로 저장 후 사용. 병합셀이 있는 표 수정 시 `propose_edit` 대신 `propose_cell_edit`을 사용.
- 검증: rhwp 실문서(병합 표·양식·양식 개체 5종)로 편집 후 한컴오피스 한글 Viewer에서 정상 렌더·병합 보존·양식 개체 값 반영 확인.
- 알려진 제약: 표 안에 중첩된 표의 셀은 좌표/라벨로 지정 불가(kordoc이 중첩 표를 구조로 노출하지 않아 에이전트가 발견할 수 없음).

## 0.3.0

### Minor Changes

- 대화 컨텍스트 자동 관리 — 토큰 예산(`max-context-tokens`, 기본 120k) 초과 시 오래된 도구 결과를 자동 축약(최근 턴·승인 짝 보존)
- 컨텍스트 사용량 표시 — 매 응답 푸터(`컨텍스트: N/예산 (%)`) + `/context` 명령(70%/90% 경고색)
- `read_document` 효율 읽기 모드 — `outline`(구조만)·`search`(키워드 주변만)으로 큰 문서를 부분 열람
- 세션 재개 UX — `sessions` 첫 메시지 미리보기·최근순, `--resume [id]`(생략 시 목록 선택)
- 채팅 진행 표시 — MCP 연결·툴 실행 스피너 및 소요 시간(비 TTY 환경 가드)

### Patch Changes

- 승인 흐름을 1단계(승인/거절)로 단순화하고, 거절 시 동일 제안 자동 반복 루프 제거
- `read_document` 100MB 파일 크기 가드(OOM/행 방지)
- MCP 툴 수 경고를 채팅·단발 질의에서도 표시
- 비 TTY 환경 스피너 이스케이프 출력 가드, stdio MCP 잔존으로 인한 종료 지연(`/exit`) 수정

## 0.2.0

### Minor Changes

- `compare_documents` 툴 추가 — 두 문서(HWP/HWPX/DOCX/XLSX, 크로스 포맷) 블록 단위 비교
- `read_document`이 `.md`/`.txt` 평문 텍스트를 kordoc 없이 직접 처리
- 기본 모델 변경(비용·속도 균형): Anthropic `claude-sonnet-4-6`, OpenAI `gpt-5.4`
- CI 자동 발행을 npm OIDC Trusted Publishing으로 전환(장기 토큰·2FA 불필요)

### Patch Changes

- 모델 API 오류 시 AI SDK 원시 객체 콘솔 덤프 제거(`onError`)
- MCP stdio 첫 연결 타임아웃 60초 상향 — `npx` 최초 다운로드 실패(법령 서버) 해소 + 안내
- `ANTHROPIC_BASE_URL`에 `/v1` 누락 시 보정 — Claude Code/Desktop 환경 footgun(404) 방지
- 실키 E2E 검증: OpenAI·Google·Anthropic 3사 채팅+툴콜, 법령 MCP(korean-law) 연동

## 0.1.0

### Minor Changes

- fc216c5: v0.1.0 첫 릴리스: 에이전트 루프(BYOK 3사), HWP/HWPX/DOCX/XLSX 읽기·쓰기(미리보기+승인), MCP 클라이언트(korean-law 기본 번들), OTA 업데이트 체크

### Patch Changes

- Updated dependencies [fc216c5]
  - @kodocagent/core@0.1.0
  - @kodocagent/doc-tools@0.1.0
  - @kodocagent/shared@0.1.0
