diff --git a/dist/chunk-DK2IE43H.js b/dist/chunk-DK2IE43H.js
index 62fd2fa13d611151a93e476073ec49f5d905f12b..396fee1cfc5b604204a61e5acb4a716b4a7d9334 100755
--- a/dist/chunk-DK2IE43H.js
+++ b/dist/chunk-DK2IE43H.js
@@ -1803,11 +1803,11 @@ function extractParagraphInfo(para, styleMap, ctx) {
   };
   walk(para);
   const leaderIdx = text.indexOf("");
-  if (leaderIdx >= 0) text = text.substring(0, leaderIdx);
+  if (leaderIdx >= 0) text = text.split(String.fromCharCode(31)).join(" ");
   let cleanText = text.replace(/[ \t]+/g, " ").trim();
   if (/^그림입니다\.?\s*원본\s*그림의\s*(이름|크기)/.test(cleanText)) cleanText = "";
-  cleanText = cleanText.replace(/그림입니다\.?\s*원본\s*그림의\s*(이름|크기)[^\n]*(\n[^\n]*원본\s*그림의\s*(이름|크기)[^\n]*)*/g, "").trim();
-  cleanText = cleanText.replace(/(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|선|직선|곡선|화살표|오각형|육각형|팔각형|별|십자|구름|마름모|도넛|평행사변형|사다리꼴|개체|그리기\s?개체|묶음\s?개체|글상자|표|그림|OLE\s?개체)\s?입니다\.?/g, "").trim();
+  cleanText = cleanText.replace(/^그림입니다\.?\s*원본\s*그림의\s*(이름|크기)[^\n]*(\n[^\n]*원본\s*그림의\s*(이름|크기)[^\n]*)*/g, "").trim();
+  cleanText = cleanText.replace(/^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|선|직선|곡선|화살표|오각형|육각형|팔각형|별|십자|구름|마름모|도넛|평행사변형|사다리꼴|개체|그리기\s?개체|묶음\s?개체|글상자|표|그림|OLE\s?개체)\s?입니다\.?$/g, "").trim();
   let style;
   if (styleMap && charPrId) {
     const charProp = styleMap.charProperties.get(charPrId);
@@ -20753,7 +20753,7 @@ function splitMarkdownUnits(md2) {
 function escapeGfm(text) {
   return text.replace(/~/g, "\\~");
 }
-var HWP_SHAPE_ALT_TEXT_RE = /(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?/g;
+var HWP_SHAPE_ALT_TEXT_RE = /^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?$/g;
 function sanitizeText(text) {
   let result = mapPuaText(text).replace(/[\u{F0000}-\u{FFFFD}]/gu, "").replace(HWP_SHAPE_ALT_TEXT_RE, "").replace(/  +/g, " ").trim();
   if (result.length <= 30 && result.includes(" ")) {
@@ -21410,7 +21410,7 @@ function buildOrigUnits(blocks) {
     let chunk;
     if (block.type === "paragraph" && block.text && /^\[별표\s*\d+/.test(sanitizeText(block.text))) {
       const next = blocks[i + 1];
-      if (next?.type === "paragraph" && next.text && /관련\)?$/.test(next.text)) consume = 2;
+      if (next?.type === "paragraph" && next.text && /^\([^)]*조[^)]*관련\)$/.test(next.text)) consume = 2;
       chunk = blocksToMarkdown(blocks.slice(i, i + consume));
     } else {
       chunk = blocksToMarkdown([block]);
diff --git a/dist/chunk-VEB43O2L.js b/dist/chunk-VEB43O2L.js
index d22bdce912032030a565e61f6726e646af8099eb..c7e02219040825e953a6668e3546c67d9f649544 100644
--- a/dist/chunk-VEB43O2L.js
+++ b/dist/chunk-VEB43O2L.js
@@ -345,7 +345,7 @@ function convertTableToText(rows) {
 function escapeGfm(text) {
   return text.replace(/~/g, "\\~");
 }
-var HWP_SHAPE_ALT_TEXT_RE = /(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?/g;
+var HWP_SHAPE_ALT_TEXT_RE = /^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?$/g;
 function sanitizeText(text) {
   let result = mapPuaText(text).replace(/[\u{F0000}-\u{FFFFD}]/gu, "").replace(HWP_SHAPE_ALT_TEXT_RE, "").replace(/  +/g, " ").trim();
   if (result.length <= 30 && result.includes(" ")) {
@@ -435,7 +435,7 @@ function blocksToMarkdown(blocks) {
       if (!text) continue;
       if (/^\[별표\s*\d+/.test(text)) {
         const nextBlock = blocks[i + 1];
-        if (nextBlock?.type === "paragraph" && nextBlock.text && /관련\)?$/.test(nextBlock.text)) {
+        if (nextBlock?.type === "paragraph" && nextBlock.text && /^\([^)]*조[^)]*관련\)$/.test(nextBlock.text)) {
           lines.push("", `## ${text} ${nextBlock.text}`, "");
           i++;
         } else {
diff --git a/dist/chunk-WEAP42T3.js b/dist/chunk-WEAP42T3.js
index cd7e5260402ec551f344bd80ed428d1b7c58ac0a..19cd4748121949733c4ea2aabe8e9cc5c8027f42 100755
--- a/dist/chunk-WEAP42T3.js
+++ b/dist/chunk-WEAP42T3.js
@@ -351,7 +351,7 @@ function convertTableToText(rows) {
 function escapeGfm(text) {
   return text.replace(/~/g, "\\~");
 }
-var HWP_SHAPE_ALT_TEXT_RE = /(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?/g;
+var HWP_SHAPE_ALT_TEXT_RE = /^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?$/g;
 function sanitizeText(text) {
   let result = mapPuaText(text).replace(/[\u{F0000}-\u{FFFFD}]/gu, "").replace(HWP_SHAPE_ALT_TEXT_RE, "").replace(/  +/g, " ").trim();
   if (result.length <= 30 && result.includes(" ")) {
@@ -441,7 +441,7 @@ function blocksToMarkdown(blocks) {
       if (!text) continue;
       if (/^\[별표\s*\d+/.test(text)) {
         const nextBlock = blocks[i + 1];
-        if (nextBlock?.type === "paragraph" && nextBlock.text && /관련\)?$/.test(nextBlock.text)) {
+        if (nextBlock?.type === "paragraph" && nextBlock.text && /^\([^)]*조[^)]*관련\)$/.test(nextBlock.text)) {
           lines.push("", `## ${text} ${nextBlock.text}`, "");
           i++;
         } else {
diff --git a/dist/chunk-XZTDYOZV.cjs b/dist/chunk-XZTDYOZV.cjs
index 99154cfe3929b0a5d406d572b5be4c7b368efb12..36db971d9e646ff52d6307a824599686f7e4d429 100644
--- a/dist/chunk-XZTDYOZV.cjs
+++ b/dist/chunk-XZTDYOZV.cjs
@@ -345,7 +345,7 @@ function convertTableToText(rows) {
 function escapeGfm(text) {
   return text.replace(/~/g, "\\~");
 }
-var HWP_SHAPE_ALT_TEXT_RE = /(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?/g;
+var HWP_SHAPE_ALT_TEXT_RE = /^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?$/g;
 function sanitizeText(text) {
   let result = mapPuaText(text).replace(/[\u{F0000}-\u{FFFFD}]/gu, "").replace(HWP_SHAPE_ALT_TEXT_RE, "").replace(/  +/g, " ").trim();
   if (result.length <= 30 && result.includes(" ")) {
@@ -435,7 +435,7 @@ function blocksToMarkdown(blocks) {
       if (!text) continue;
       if (/^\[별표\s*\d+/.test(text)) {
         const nextBlock = blocks[i + 1];
-        if (_optionalChain([nextBlock, 'optionalAccess', _20 => _20.type]) === "paragraph" && nextBlock.text && /관련\)?$/.test(nextBlock.text)) {
+        if (_optionalChain([nextBlock, 'optionalAccess', _20 => _20.type]) === "paragraph" && nextBlock.text && /^\([^)]*조[^)]*관련\)$/.test(nextBlock.text)) {
           lines.push("", `## ${text} ${nextBlock.text}`, "");
           i++;
         } else {
diff --git a/dist/index.cjs b/dist/index.cjs
index 5c5f9b3b34fc029e248b364ef6723d2816a790e0..0fb9a45fa4ff19c868dd0cc291824c2ca2f19e22 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -2038,11 +2038,11 @@ function extractParagraphInfo(para, styleMap, ctx) {
   };
   walk(para);
   const leaderIdx = text.indexOf("");
-  if (leaderIdx >= 0) text = text.substring(0, leaderIdx);
+  if (leaderIdx >= 0) text = text.split(String.fromCharCode(31)).join(" ");
   let cleanText = text.replace(/[ \t]+/g, " ").trim();
   if (/^그림입니다\.?\s*원본\s*그림의\s*(이름|크기)/.test(cleanText)) cleanText = "";
-  cleanText = cleanText.replace(/그림입니다\.?\s*원본\s*그림의\s*(이름|크기)[^\n]*(\n[^\n]*원본\s*그림의\s*(이름|크기)[^\n]*)*/g, "").trim();
-  cleanText = cleanText.replace(/(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|선|직선|곡선|화살표|오각형|육각형|팔각형|별|십자|구름|마름모|도넛|평행사변형|사다리꼴|개체|그리기\s?개체|묶음\s?개체|글상자|표|그림|OLE\s?개체)\s?입니다\.?/g, "").trim();
+  cleanText = cleanText.replace(/^그림입니다\.?\s*원본\s*그림의\s*(이름|크기)[^\n]*(\n[^\n]*원본\s*그림의\s*(이름|크기)[^\n]*)*/g, "").trim();
+  cleanText = cleanText.replace(/^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|선|직선|곡선|화살표|오각형|육각형|팔각형|별|십자|구름|마름모|도넛|평행사변형|사다리꼴|개체|그리기\s?개체|묶음\s?개체|글상자|표|그림|OLE\s?개체)\s?입니다\.?$/g, "").trim();
   let style;
   if (styleMap && charPrId) {
     const charProp = styleMap.charProperties.get(charPrId);
@@ -20974,7 +20974,7 @@ function splitMarkdownUnits(md2) {
 function escapeGfm(text) {
   return text.replace(/~/g, "\\~");
 }
-var HWP_SHAPE_ALT_TEXT_RE = /(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?/g;
+var HWP_SHAPE_ALT_TEXT_RE = /^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?$/g;
 function sanitizeText(text) {
   let result = _chunkXZTDYOZVcjs.mapPuaText.call(void 0, text).replace(/[\u{F0000}-\u{FFFFD}]/gu, "").replace(HWP_SHAPE_ALT_TEXT_RE, "").replace(/  +/g, " ").trim();
   if (result.length <= 30 && result.includes(" ")) {
@@ -21631,7 +21631,7 @@ function buildOrigUnits(blocks) {
     let chunk;
     if (block.type === "paragraph" && block.text && /^\[별표\s*\d+/.test(sanitizeText(block.text))) {
       const next = blocks[i + 1];
-      if (_optionalChain([next, 'optionalAccess', _198 => _198.type]) === "paragraph" && next.text && /관련\)?$/.test(next.text)) consume = 2;
+      if (_optionalChain([next, 'optionalAccess', _198 => _198.type]) === "paragraph" && next.text && /^\([^)]*조[^)]*관련\)$/.test(next.text)) consume = 2;
       chunk = _chunkXZTDYOZVcjs.blocksToMarkdown.call(void 0, blocks.slice(i, i + consume));
     } else {
       chunk = _chunkXZTDYOZVcjs.blocksToMarkdown.call(void 0, [block]);
diff --git a/dist/index.js b/dist/index.js
index 41d7debe128cf68fea74d9ad20ef8787b0c76955..b58a1b824861dd4102e03499bf4944ba568b4e8d 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -2035,11 +2035,11 @@ function extractParagraphInfo(para, styleMap, ctx) {
   };
   walk(para);
   const leaderIdx = text.indexOf("");
-  if (leaderIdx >= 0) text = text.substring(0, leaderIdx);
+  if (leaderIdx >= 0) text = text.split(String.fromCharCode(31)).join(" ");
   let cleanText = text.replace(/[ \t]+/g, " ").trim();
   if (/^그림입니다\.?\s*원본\s*그림의\s*(이름|크기)/.test(cleanText)) cleanText = "";
-  cleanText = cleanText.replace(/그림입니다\.?\s*원본\s*그림의\s*(이름|크기)[^\n]*(\n[^\n]*원본\s*그림의\s*(이름|크기)[^\n]*)*/g, "").trim();
-  cleanText = cleanText.replace(/(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|선|직선|곡선|화살표|오각형|육각형|팔각형|별|십자|구름|마름모|도넛|평행사변형|사다리꼴|개체|그리기\s?개체|묶음\s?개체|글상자|표|그림|OLE\s?개체)\s?입니다\.?/g, "").trim();
+  cleanText = cleanText.replace(/^그림입니다\.?\s*원본\s*그림의\s*(이름|크기)[^\n]*(\n[^\n]*원본\s*그림의\s*(이름|크기)[^\n]*)*/g, "").trim();
+  cleanText = cleanText.replace(/^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|선|직선|곡선|화살표|오각형|육각형|팔각형|별|십자|구름|마름모|도넛|평행사변형|사다리꼴|개체|그리기\s?개체|묶음\s?개체|글상자|표|그림|OLE\s?개체)\s?입니다\.?$/g, "").trim();
   let style;
   if (styleMap && charPrId) {
     const charProp = styleMap.charProperties.get(charPrId);
@@ -20971,7 +20971,7 @@ function splitMarkdownUnits(md2) {
 function escapeGfm(text) {
   return text.replace(/~/g, "\\~");
 }
-var HWP_SHAPE_ALT_TEXT_RE = /(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?/g;
+var HWP_SHAPE_ALT_TEXT_RE = /^(?:모서리가 둥근 |둥근 )?(?:사각형|직사각형|정사각형|원|타원|삼각형|이등변 삼각형|직각 삼각형|선|직선|곡선|화살표|굵은 화살표|이중 화살표|오각형|육각형|팔각형|별|[4-8]점별|십자|십자형|구름|구름형|마름모|도넛|평행사변형|사다리꼴|부채꼴|호|반원|물결|번개|하트|빗금|블록 화살표|수식|표|그림|개체|그리기\s?개체|묶음\s?개체|글상자|수식\s?개체|OLE\s?개체)\s?입니다\.?$/g;
 function sanitizeText(text) {
   let result = mapPuaText(text).replace(/[\u{F0000}-\u{FFFFD}]/gu, "").replace(HWP_SHAPE_ALT_TEXT_RE, "").replace(/  +/g, " ").trim();
   if (result.length <= 30 && result.includes(" ")) {
@@ -21628,7 +21628,7 @@ function buildOrigUnits(blocks) {
     let chunk;
     if (block.type === "paragraph" && block.text && /^\[별표\s*\d+/.test(sanitizeText(block.text))) {
       const next = blocks[i + 1];
-      if (next?.type === "paragraph" && next.text && /관련\)?$/.test(next.text)) consume = 2;
+      if (next?.type === "paragraph" && next.text && /^\([^)]*조[^)]*관련\)$/.test(next.text)) consume = 2;
       chunk = blocksToMarkdown(blocks.slice(i, i + consume));
     } else {
       chunk = blocksToMarkdown([block]);
